{
    "name": "lexia",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "esbuild ./src/**/* --bundle  --outdir=dist --minify --sourcemap",
        "dev": "esbuild ./src --bundle  --outdir=dist --watch --sourcemap",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "esbuild": "^0.19.5",
        "tsc": "^2.0.4",
        "typescript": "^5.2.2"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.253"
    }
}
